# Design reference routing

`style-skills/` contains the style skills copied from `take/.claude/skills/` and organized into one directory per style. The parent `SKILL.md` routes design tasks to only the relevant child references.
